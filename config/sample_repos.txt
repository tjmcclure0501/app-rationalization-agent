# sample_repos.txt — batch input for the rationalization agent
# One repo per line, owner/name format
# Lines starting with # are ignored

# Classifier type → expected recommendation per the repo_classifier
# taxonomy (see repo_classifier.py + repo_type_overrides in scoring_rubric.json).

facebook/react
redmine/redmine
atom/atom
getpatchwork/patchwork
